import 'jest';

describe('Bluethooth Connector', function() {
    it('can start a scan', () => {
        expect(0).toBe(0);
    });
    it('can stop a scan', () => {
        expect(0).toBe(1);
    });
    it('can connect a thing', () => {
        expect(0).toBe(1);
    });
    it('can disconnect a thing', () => {
        expect(0).toBe(1);
    });
});